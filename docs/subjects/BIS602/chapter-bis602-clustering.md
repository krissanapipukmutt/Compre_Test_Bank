# Clustering

## การจัดกลุ่มข้อมูล

### Concise summary

Clustering is unsupervised learning that groups observations by similarity; k-means iteratively assigns points to centroids and updates those centroids.

### สรุปภาษาไทย

Clustering เป็นการเรียนรู้แบบไม่มี Label เพื่อจัดกลุ่มตามความคล้าย โดย k-means จะวนกำหนดจุดให้ Centroid และปรับ Centroid ใหม่

### คำอธิบายโดยละเอียด

Clustering เป็นการเรียนรู้แบบไม่มี Label เพื่อจัดกลุ่มตามความคล้าย โดย k-means จะวนกำหนดจุดให้ Centroid และปรับ Centroid ใหม่ ให้เริ่มจากวัตถุประสงค์ทางธุรกิจ ระบุข้อมูลหรือเงื่อนไขที่ต้องใช้ เลือกแนวคิดหรือขั้นตอนให้ตรงกับโจทย์ ตรวจสมมติฐาน แล้วสื่อความหมายของผลลัพธ์และข้อจำกัดอย่างชัดเจน

### Technical terms and definitions

- **Unsupervised learning / การเรียนรู้แบบไม่มีผู้สอน** — Learning structure from observations without target labels.  
  การเรียนรู้โครงสร้างจากข้อมูลโดยไม่มี Label เป้าหมาย
- **K-means / เคมีนส์** — A centroid-based algorithm minimizing within-cluster squared distance.  
  อัลกอริทึมอิงจุดศูนย์กลางที่ลดระยะกำลังสองภายในกลุ่ม
- **Cluster validation / การตรวจสอบคุณภาพกลุ่ม** — Assess whether groups are compact, separated, stable, and useful.  
  ประเมินว่ากลุ่มกระชับ แยกจากกัน เสถียร และใช้ประโยชน์ได้หรือไม่

### Processes and frameworks

- Identify the purpose and boundary of Clustering.
- Apply Unsupervised learning, K-means, Cluster validation in the order required by the case.
- Check assumptions, evidence quality, and stakeholder consequences.

### Formulas

- No primary formula is required for this chapter.

### Example

- Apply clustering to a supplied exercise or business case and justify each choice from the source material.

### Comparison

- Distinguish `Unsupervised learning` from `K-means` by purpose, input, and decision use.

### Common misunderstandings

- Treating Unsupervised learning as a label to memorize instead of explaining its decision purpose.
- Presenting a result without checking assumptions, scope, or evidence limitations.

### Likely examination points

- Define and distinguish Unsupervised learning, K-means, Cluster validation.
- Apply the chapter framework to a short case and justify the selected concept.
- Interpret the result, limitation, or next action.

### Study aids

- Review: Clustering is unsupervised learning that groups observations by similarity; k-means iteratively assigns points to centroids and updates those centroids.
- Memory aid: **Purpose → evidence → method → result → limitation.**
- Review questions:
  - What decision problem does Clustering address?
  - How do Unsupervised learning and K-means differ?
  - Which assumptions or limitations should be disclosed?

### Sources

- `file-8ab03dba6a772847` — `TERM1/BIS602/Final/Lacture/L10 Clustering.pdf`, pp./slides 1–21

### Evidence

- Confidence: `high`
- Evidence type: `summarized_from_source`
